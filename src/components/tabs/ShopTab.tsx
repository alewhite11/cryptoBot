import React from 'react';

interface ShopTabProps {
    
}

const ShopTab: React.FC<ShopTabProps> = ({  }) => {
    return (
      <div className="App">
        <header className="App-header" style={{ position: 'relative', width: '100%' }}>
          <h1>Shop:</h1>
        </header>
      </div>
    );
  };
  
  export default ShopTab;